---
import { turso } from "src/turso";

const bankAccountID = Astro.url.searchParams.get("bankAccountID");

await turso.execute(`UPDATE BankAccounts SET statusID = 17 WHERE bankAccountID = ${bankAccountID}`);

return Astro.redirect('/bankaccounts')
---