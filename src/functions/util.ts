// utils.ts (si decides crear un archivo para las funciones)
export function generatCustomerID(): number {
    return Date.now(); // Genera un ID único basado en el tiempo actual
  }
  
  export function generateReservationID(): number {
    return Date.now(); // Genera un ID único para cada reserva
  }

  